{
  "name": "briefr-meeting-bot",
  "version": "1.0.0",
  "type": "module",
  "description": "Playwright bot that joins Google Meet, captures audio, and ships transcripts to the Briefr backend via local Whisper STT.",
  "main": "server.js",
  "scripts": {
    "test": "node --test",
    "start": "node server.js",
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "1.62.1",
    "node-fetch": "^3.3.2",
    "form-data": "^4.0.0",
    "dotenv": "^16.4.5",
    "wav-encoder": "^1.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
